#include <stdio.h>  /* stdio, stdout, fgetc(), fputc() */
#include <stdlib.h> /* malloc(), realloc() */
#include <string.h> /* strcmp() */

#define streq(a,b) (strcmp(a,b) == 0)


#include "parseInput.h"


int main( int argc, char**argv);

